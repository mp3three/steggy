{
  "configuration": {
    "ALBUM_DOWNLOAD_TARGET": {
      "type": "string",
      "description": "Base folder for downloading",
      "default": "~/Downloads"
    },
    "ALBUM_PAD_SIZE": {
      "type": "number",
      "description": "Pad numbers in file names to a consistent size so they can be sorted more easily",
      "default": 4
    },
    "CONTROLLER_API": {
      "type": "string",
      "description": "HTTP API target for the home controller",
      "default": "http://localhost:7000"
    }
  }
}
