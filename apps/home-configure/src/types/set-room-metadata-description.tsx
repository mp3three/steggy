import { Typography } from 'antd';

export const NUMBER_FORMULA_DESCRIPTION = (
  <Typography.Paragraph>
    <Typography.Text>
      Enter a math expression, the result will be set as the new metadata value
    </Typography.Text>
  </Typography.Paragraph>
);
