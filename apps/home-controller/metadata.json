{
  "configuration": {
    "SAFE_MODE": {
      "type": "boolean",
      "default": false,
      "description": "No "
    },
    "NOTIFY_UNAVAILABLE_DURATION": {
      "type": "number",
      // 4 hours = 1000 * 60 * 60 * 4
      "default": 14400000,
      "description": "Raise error if entity is unavailable for this long"
    },
    "UNAVAILABLE_MONITOR_HOUR": {
      "type": "number",
      "default": 11,
      "description": "When to send notifications about unavailable entities"
    },
    "SEQUENCE_TIMEOUT": {
      "type": "number",
      "description": "When tracking state changes for a kunami event, another change must happen inside this time window",
      "default": 1500
    },
    "CIRCADIAN_MAX_TEMP": {
      "type": "number",
      "default": 5500,
      "description": "Maximum color temperature for circadian lighting. Used at solar noon"
    },
    "CIRCADIAN_MIN_TEMP": {
      "type": "number",
      "default": 2000,
      "description": "Minimum color temperature for circadian lighting. Used while it's dark out"
    },
    "MIN_BRIGHTNESS": {
      "type": "number",
      "default": 5,
      "description": "Enforce a number higher than 1 for min brightness in dimmers. Some lights do weird stuff at low numbers"
    },
    "MONGO_URI": {
      "type": "string",
      "default": "mongodb://localhost:27017/automagical",
      "description": "Mongo connection string"
    },
    "MONGO_CERT": {
      "type": "string",
      "description": "Optional configuration item, used with mongo ssl connections. Provide value as absolute file path",
      "careful": true
    },
    "MONGO_KEY": {
      "type": "string",
      "description": "Optional configuration item, used with mongo ssl connections. Provide value as absolute file path",
      "careful": true
    },
    "MONGO_CA": {
      "type": "string",
      "description": "Optional configuration item, used with mongo ssl connections. Provide value as absolute file path",
      "careful": true
    },
    "MONGO_CRL": {
      "type": "string",
      "description": "Optional configuration item, used with mongo ssl connections. Provide value as absolute file path",
      "careful": true
    }
  }
}
