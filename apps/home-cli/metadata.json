{
  "configuration": {
    "CONTROLLER_API": {
      "type": "string",
      "description": "HTTP API target for the home controller",
      "default": "http://localhost:7000"
    },
    "ADMIN_KEY": {
      "type": "password",
      "description": "Admin key for the home controller"
    },
    "REFRESH_SLEEP": {
      "type": "number",
      "default": 100,
      "description": "Delay before loading entity header for ensuring state is up to date"
    }
  }
}
