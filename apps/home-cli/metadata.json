{
  "configuration": {
    "CONTROLLER_API": {
      "type": "string",
      "description": "HTTP API target for the home controller",
      "default": "http://localhost:7000"
    },
    "ADMIN_KEY": {
      "type": "password",
      "description": "Admin key for the home controller"
    },
    "REFRESH_SLEEP": {
      "type": "number",
      "default": 100,
      "description": "Delay before loading entity header for ensuring state is up to date"
    },
    "CLI_PACKAGE": {
      "type": "string",
      "default": "https://raw.githubusercontent.com/ccontour/for-science/master/apps/home-cli/package.json",
      "description": "Used for determining if running code is latest"
    },
    "CONTROLLER_PACKAGE": {
      "type": "string",
      "default": "https://raw.githubusercontent.com/ccontour/for-science/master/apps/home-controller/package.json",
      "description": "Used for determining if running code is latest"
    }
  }
}
