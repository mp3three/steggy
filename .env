# Master list of variables, descriptions, and instructions on registering new variables
# ## libs/logger/src/lib/globals.ts

# This file sets up default values.
# To set your own, create a `.local.env` instead in either:
# - apps/{app_name}/.local.env
# - Repository root
# 
# .env resolution order (first come first serve):
# apps/{app_name}/.local.env
# apps/{app_name}/.env
# .local.env
# .env

# Controls logging
DEBUG=*,-avvio,-mqtt-packet*,-express*,-automagical:SocketService:debug
