{
  "$id": "metadata.json",
  "title": "automagical",
  "description": "Additional metadata for applications and libraries. Information that is not described by `package.json` or `project.json`",
  "type": "object",
  "properties": {
    "configuration": {
      "$ref": "./config.schema.json"
    }
  },
  "additionalProperties": false
}
