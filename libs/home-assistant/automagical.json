{
  "configuration": {
    "TOKEN": {
      "type": "password",
      "required": true,
      "description": "Auth token to access Home Assistant"
    },
    "BASE_URL": {
      "type": "url",
      "default": "http://localhost:8123",
      "description": "Url to reach Home Assistant at"
    },
    "WEBSOCKET_URL": {
      "type": "string",
      "description": "Override calculated value if it's breaking or you want something custom. Make sure to use wss://",
      "careful": true
    },
    "WARN_REQUESTS_PER_SEC": {
      "type": "number",
      "default": 300,
      "description": "Emit warnings if the home controller attempts to send more than X messages to Home Assistant inside of a second"
    },
    "CRASH_REQUESTS_PER_SEC": {
      "type": "number",
      "default": 500,
      "description": "Socket service will self-terminate the process if this limit is exceeded. This can happen if an accidential infinite recursion is triggered due to bad setup"
    },
    "RENDER_TIMEOUT": {
      "type": "number",
      "default": 3,
      "description": "TODO: add description"
    }
  }
}
