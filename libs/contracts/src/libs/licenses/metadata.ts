import { TrackedItemDTO } from './tracked-item.dto';

export class LicenseMetadataDTO {
  // #region Object Properties

  public apiServer?: TrackedItemDTO[];
  public trackedItems?: TrackedItemDTO[];

  // #endregion Object Properties
}
