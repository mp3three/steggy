{
  "root": "libs/controller-logic",
  "sourceRoot": "libs/controller-logic/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/node:package",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/controller-logic",
        "tsConfig": "libs/controller-logic/tsconfig.lib.json",
        "packageJson": "libs/controller-logic/package.json",
        "main": "libs/controller-logic/src/index.ts",
        "assets": ["libs/controller-logic/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["libs/controller-logic/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/controller-logic"],
      "options": {
        "jestConfig": "libs/controller-logic/jest.config.js",
        "passWithNoTests": true
      }
    },
    "publish": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "npx nx build controller-logic --configuration=production",
            "description": "Create production optimized build"
          },
          {
            "command": "terser dist/apps/controller-logic/main.js -c -m --keep-classnames --module > dist/apps/controller-logic/main.js",
            "description": "Minify it (in place)"
          },
          {
            "command": "yarn publish --cwd dist/apps/controller-logic --non-interactive",
            "description": "Publish it"
          }
        ]
      }
    }
  },
  "tags": ["nestjs"]
}
