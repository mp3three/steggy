{
  "root": "libs/rcon",
  "sourceRoot": "libs/rcon/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/rcon",
        "tsConfig": "libs/rcon/tsconfig.lib.json",
        "buildableProjectDepsInPackageJsonType": "peerDependencies",
        "updateBuildableProjectDepsInPackageJson": true,
        "packageJson": "libs/rcon/package.json",
        "main": "libs/rcon/src/index.ts",
        "assets": ["libs/rcon/*.md", "libs/rcon/package.json"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": false,
          "fileReplacements": []
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/rcon/**/*.ts"]
      }
    },
    "publish": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "npx figlet-cli -f Pagga Rules Engine",
            "description": "Announce it"
          },
          {
            "command": "npx nx build rcon --configuration=production",
            "description": "Create production optimized build"
          },
          {
            "command": "yarn publish --cwd dist/libs/rcon --non-interactive",
            "description": "Publish it"
          }
        ]
      }
    }
  },
  "tags": []
}
